language: ruby
rvm:
  - 2.0.0-p353
env:
  - GOVUK_APP_DOMAIN=test.gov.uk RUBYOPT="-rpsych"
script:
  - bundle exec rake
bundler_args: --without=development
branches:
  except:
    - release
notifications:
  email: false
